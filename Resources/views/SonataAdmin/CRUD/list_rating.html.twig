{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{% block field %}
    {% if field_description.options.range is defined %}
        {{ value|format_rating(field_description.options.range)|number_format }}
    {% else %}
        {{ value|format_rating()|number_format }}
    {% endif %}
{% endblock %}